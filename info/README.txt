
Files /default.ini.copy and /local.ini.copy are copies of the configuration files used to initialize CouchDB. The copies are NOT used to initialize CouchDB. 

To alter the CouchDB configuration, add .ini files within the local.d folder. 

For more info see: https://github.com/dma-dk/embryo-couchdb
